{{- if eq (include "v-learning.istioEnabled" .) "true" }}
apiVersion: networking.istio.io/v1beta1
kind: VirtualService
metadata:
  name: v-learning-vs
  namespace: {{ .Values.global.namespace }}
spec:
  hosts:
  - {{ .Values.istio.ingressGateway.host }}
  gateways:
  - {{ .Values.istio.ingressGateway.name }}
  http:
  # All routes redirected to API Gateway
  - match:
    - uri:
        prefix: /
    route:
    - destination:
        host: api-gateway
        port:
          number: {{ .Values.gateway.port }}
    corsPolicy:
      allowCredentials: true
      allowHeaders:
      - Authorization
      - Content-Type
      - Accept
      - Origin
      - X-Requested-With
      allowMethods:
      - GET
      - POST
      - PUT
      - DELETE
      - PATCH
      - OPTIONS
      allowOrigins:
      - exact: '*'
      maxAge: 24h
{{- end }}
